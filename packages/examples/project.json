{
  "name": "fluent-ffmpeg-examples",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/fluent-ffmpeg-examples",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'No build step needed for examples'",
        "cwd": "{projectRoot}"
      }
    }
  },
  "tags": ["examples", "demo"]
}
